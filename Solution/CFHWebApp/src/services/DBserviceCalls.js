"use strict";

var React = require('react');

var SaveToDB = React.createClass({
    saveNewUser: function(newUser){
        console.log("saveNewUser is being called");
        return(
            newUser
        );
    }
});